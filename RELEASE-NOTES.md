# ImanJo — نسخة الإصلاح 2026-09-06

هذه نسخة مصدرية محسنة مبنية على Frontend.zip وback(10).zip اللتين قدمتهما. لا تتضمن نشرًا على خادمك ولا ضمانًا مطلقًا بالخلو من الأخطاء. اقرأ تعليمات الترقية قبل استبدال النسخة الحالية.

## نتائج التحقق الفعلية

- نجحت 19 حالة اختبار Node للواجهة. تشمل سياسة Origin، أعمار كوكيز JWT، استعادة جلسة refresh دون access cookie، تجديدًا متزامنًا واحدًا، الحفاظ على الجلسة عند خطأ 503، صحة بلد وحد دفعة الإصلاح، حدود جسم الصورة ومسارها، وحفظ المقال مع تحذير واضح عند تعطل الملف/SEO.
- نجح Astro check على 296 ملفًا: صفر أخطاء، صفر تحذيرات، وصفر تلميحات.
- نجح Astro production build باستخدام API تجريبي محلي.
- نجحت 8 فحوص HTTP على النسخة المبنية: الرئيسية، الدخول، صفحة معلومات، robots، رفض POST خارجي، إعادة ضيف التنزيل لتسجيل الدخول، رفض مسار صورة متجاوز، وإخراج WebP صالح.
- اجتاز المصدر Go فحصًا نحويًا عبر Tree-sitter لجميع ملفات Go، وتنسيق الملفات المتغيرة عبر gofmt المبني إلى WASM. هذا ليس تجميع Go أو تحليل أنواع أو تنفيذ الاختبارات.
- Go غير متوفر في هذه البيئة، ومحاولة الوصول لتنزيله لم تتم بسبب قيود الشبكة. لم يتم تنفيذ go build/go test/go vet أو اختبارات MySQL هنا. لا تعتبر النسخة معتمدة للنشر قبل نجاح هذه البوابة على staging/CI.
- أضيفت اختبارات Go للحماية والمسارات، واختبارات MySQL لاعتماد اشتراك متزامن، وrollback عند الفشل، وحجز آخر رصيد تنزيل بالتزامن وتعويض الفشل. اختبارات MySQL تستخدم قاعدة مؤقتة جديدة ولا تستخدم قاعدة التطبيق.
- ملفات workflow للطرفين موجودة في .github/workflows/verify.yml، لكنها لم تُرفع إلى GitHub أو تُشغّل هناك خلال هذه المهمة.

## ما تغير بالنسبة إلى التقرير السابق

| المرجع | المعالجة المصدرية |
|---|---|
| F01–F02 | إزالة كاش استجابات API الذي سبق الحماية؛ no-store للمخرجات؛ رموز التنزيل لا تُعاد من نسخة قديمة. |
| F03 | سياسة PublicFileByID/IsPublicFile تمنع كشف المسودات والأصول المعطلة والملفات اليتيمة/المدفوعة؛ مسار إداري مستقل. |
| F04 | /storage يقدم الصور العامة وsitemaps فقط؛ يمنع private وfiles والوثائق؛ Nginx يمرر الطلب إلى Go بدل alias؛ المرفقات الجديدة تحت private/attachments؛ إلغاء fallback إلى ملف عام. |
| F05 | الدفعات مقيدة بدولة سياق الطلب، وترفض تعارض الجسم والترويسة؛ إرسال country_code صراحة. ومنع الرجوع إلى قاعدة الأردن عند فشل دولة أخرى أو تعريفها بنفس قاعدة الأردن. |
| F06 | اعتماد الطلب داخل transaction بقفل السجل ورابط source_order_id فريد؛ إعادة الاعتماد ترجع نفس الاشتراك الجديد؛ إشعار وتعيين الدور ضمن المعاملة؛ رفض الطلب مشروط ببقائه pending. |
| F07 | فحص IsActive في تحميل المستخدم الاختياري وبوابة التنزيل. |
| F08 | تجديد الجلسة مع refresh وحده؛ أعمار الكوكيز مشتقة من exp؛ تجديد الجلسة قبل قراءة كوكيز BFF المحمية. |
| F09 | فحص أصل جميع طلبات التغيير مقابل PUBLIC_SITE_URL؛ fallback مضبوط إلى Referer؛ عدم الوثوق بـ X-Forwarded-Host لتحديد الأصل المسموح. |
| F10 | تعطيل كاش HTML وكاش أجسام المحتوى المتداخل؛ إبقاء كاش المرجعيات والتحليلات مع إبطال إضافي بعد الكتابة. المقايضة: قد تزيد قراءات DB ويجب قياس الحمل قبل إعادة كاش المحتوى بتصميم إصدار/إبطال موحد. |
| F11 | رفض payment_proof_url من واجهة JSON العامة؛ حصر الإثبات في مجلد صاحبه؛ فحص الحجم وMIME/الامتداد في الرفع. |
| F12 | secure/view يتطلب بريدًا مفعلًا وصلاحية manage files؛ حماية حدود التخزين تشمل symlinks. رفع الملفات العام يتطلب upload files. |
| F13 | قوالب الخدمة تستخدم مستخدم imanjo؛ منافذ Docker محلية؛ إعدادات الأسرار إلزامية؛ سكربت التثبيت يجهز فقط ولا يعيد تشغيل خدمة دون ترقية. |
| F14 | endpoint مستقل للمشاهدة بدل أثر GET الجانبي؛ حد زيارة واحدة لكل IP/دولة/مقال خلال 30 دقيقة لكل عملية Go. هذا تعريف تقريبي للمشاهدة ولا يساوي عدد أشخاص فريدين؛ في عدة replicas يلزم مخزن تحديد مشترك. |
| F15 | إرسال limit=100 بدل count=100. |
| F16 | فشل SEO أو ربط المصدر يتحول إلى تحذير مع رقم المقال/المنشور المحفوظ؛ فشل شبكة رفع مرفقات بعد إنشاء مقال يوجه إلى التعديل بدل إعادة الإنشاء. لا يمثل ذلك ضمان exactly-once لكل انقطاع ممكن قبل وصول استجابة الحفظ الأساسي. |
| F17 | قراءة صور حتى 10MB، مهلة قراءة كاملة، حد pixels، أربعة تحويلات متوازية، توحيد القياسات وكاش مخرجات محدود 32MB. |
| F18 | حجز الرصيد تحت قفل الاشتراك؛ failed لا يستهلك الرصيد؛ تسجيل served عند نجاح تجهيز استجابة الملف؛ الحجوزات المتروكة تُعاد failed دوريًا بعد الانقطاع. served يعني إعداد استجابة مسموحة، وليس إثبات استلام كل بايت من العميل. |
| F19 | جدول دائم لإبطال access tokens، وفحص DB عند غياب blacklist من Redis؛ حذف السجلات المنتهية دوريًا؛ noeviction في قالب Redis. |
| F20 | --migrate-only منفصل عن تشغيل الخادم؛ سجل إصدار مخطط لكل قاعدة متاحة؛ إزالة AutoMigrate من المسارات العادية الخاصة بالتدقيق والرسائل والشات ومخزن المعلمين؛ منع rollback آلي قد يفقد بيانات. |
| F21 | scripts للاختبارات والفحص والبناء، smoke test محلي، واختبارات Go وMySQL وبوابة CI. تغطي العقود الحساسة المعدلة؛ لا تعني توليد كل أنواع الواجهة من كامل Swagger القديم. |

## خطوات الترقية

1. استخدم نسخة staging من قواعد البيانات والملفات، وخذ نسخة احتياطية قابلة للاستعادة من DB والملفات والإعدادات والبناء الحالي.
2. فك الملفات في مجلدَي المشروع. الأرشيفان يحتويان المصدر والموارد، ولا يحتويان node_modules أو dist أو أسرار .env أو ملفات النسخ الاحتياطية .bak. لا تشغّل dist القديم بعد استبدال المصدر.
3. شغّل للباك إند من مجلده:

```bash
go test ./...
go vet ./...
go build -trimpath -o fiber-api ./cmd/server
```

اختبارات التزامن تحتاج MySQL تجريبيًا وIMANJO_TEST_MYSQL_DSN بصيغة `user:password@tcp(host:3306)/?parseTime=true`. يجب أن يملك مستخدم الاختبار حق إنشاء/حذف قواعد مؤقتة. لا تستخدم حساب الإنتاج لهذه الاختبارات.

4. راجع .env الفعلي وDB لكل دولة. الدولة غير المتاحة ستعيد 503، ولن تقرأ أو تكتب في قاعدة الأردن. تحقق من ضبط أسماء/خوادم الدول النشطة بشكل مستقل.
5. قبل تشغيل الإصدار الجديد، نفّذ `fiber-api --migrate-only` بنفس EnvironmentFile وWorkingDirectory المستخدمين للخدمة. أُرفق قالب `deploy/imanjo/imanjo-api-migrate.service` لتشغيل الأمر عبر systemd دون source لملف الأسرار. اضبط مسارات القالب لتطابق موقعك الفعلي أولًا؛ لا تفترض أن مسار Plesk الافتراضي يطابق خادمك.
6. الترقية تعتمد على قاعدة المشروع الحالية؛ لا تدعي ملفات bootstrap أنها تستورد جميع بيانات ومخطط Laravel من قاعدة فارغة. ظهرت في الأصل ترحيلات legacy متفرقة؛ احتفظ بها للمراجعة، ولا تشغلها تلقائيًا بعد الأمر الجديد.
7. امنح مستخدم الخدمة حق القراءة للكود والكتابة فقط لمجلدات storage/logs المطلوبة. تحقق خاصة من STORAGE_PATH الخارجي ومجلدات storage/private نسبة إلى WorkingDirectory. القوالب الجديدة تستخدم imanjo بدل root، ولذلك يلزم تجهيز المستخدم والأذونات قبل تشغيلها. `install-backend.sh` يجهز المستخدم والقوالب، ويدعم IMANJO_APP_DIR، ولا يفعل النشر أو إعادة التشغيل تلقائيًا.
8. طبّق قواعد /storage في Nginx لكل من دومين الموقع ودومين API، وأزل أي alias أو تقديم مباشر أقدم يتجاوزها. نفّذ nginx -t قبل reload. يجب ألا تكون ملفات PDF/Word قابلة للتنزيل المباشر من /storage.
9. امسح النسخ السابقة المخزنة للمرفقات المحمية في CDN/Cloudflare إن كانت استجاباتها عامة من قبل. تعديل origin لا يسحب نسخة سبق تخزينها خارج الخادم. لا تُعد نشر الوثائق للعامة لاستعادة رابط قديم؛ صحّح موقعها الفعلي وSTORAGE_PATH بدلًا من ذلك.
10. للواجهة: اضبط PUBLIC_API_URL وINTERNAL_API_URL وPUBLIC_SITE_URL وFRONTEND_API_KEY قبل البناء. PUBLIC_SITE_URL هو الأصل المعتمد لطلبات النماذج؛ استخدم دومينًا موحدًا وأعد توجيه www إليه عند وجوده.

```bash
npm ci
npm test
npm run check
npm run build
```

`npm run smoke` يبني باستخدام بيانات محلية تجريبية ويختبر HTTP؛ لا تستخدم dist الناتج منه للنشر. بعده أعد npm run build بإعدادات الإنتاج الحقيقية.

11. شغّل النسختين المتوافقتين واختبر: تسجيل/دخول/تجديد/خروج، حساب محظور، كل دولة نشطة، مسودة بمرفق، نشر/إلغاء نشر، تنزيل ضيف حسب الإعداد، دفعة أوصاف، طلب اشتراك واعتماده، ورصيد تنزيل متزامن.
12. راقب CPU/RAM/DB وTTFB ومعدل 4xx/5xx بعد تفعيل النسخة. تعطيل كاش المحتوى يعالج الاتساق لكنه يحتاج قياسًا عند الحمل الحقيقي.

## قيود واعتماد الإصدار

- تحقق Go/MySQL الكامل وترقية staging وقياس الحمل ما زالت بوابات يجب تنفيذها خارج بيئة هذه المحادثة. لا تتجاوزها لأن parser لم يجد أخطاء نحوية.
- لا يوجد في الأرشيف اعتماد لقبول AdSense أو تفعيل CMP/GSC؛ ذلك يحتاج إعدادات الحساب واختبار متصفح فعلي.
- لم تُنقل ملفات الإنتاج القديمة أو تُغيّر أذوناتها أو قواعد Nginx/Cloudflare. أُرفق الكود والقوالب اللازمة؛ يجب مطابقتها وتهيئتها على الخادم.
- للطلبات المعتمدة قديمًا قبل وجود source_order_id: إعادة «اعتماد» طلب قديم لا تُنشئ اشتراكًا جديدًا للتعويض تلقائيًا. راجع الطلب الذي كان معتمدًا بلا اشتراك يدويًا بعد مطابقة الدفع والبيانات.
- لا أدرج ملفات البناء التجريبية حتى لا تحمل نسخة الإنتاج عناوين localhost أو مفتاح الاختبار.
